/**
 * The JamCLI version recorded in session logs. It is a constant rather than a read of
 * package.json so a compiled binary carries it too; a test keeps the two equal.
 */
export const JAMCLI_VERSION = '1.0.0';
