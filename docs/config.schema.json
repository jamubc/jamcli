{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string",
      "description": "The JSON schema this file follows, for editors."
    },
    "model": {
      "type": "string",
      "minLength": 1,
      "description": "The model sessions start on, as \"provider:model\" or a model on the profile's provider."
    },
    "active_profile": {
      "type": "string",
      "minLength": 1,
      "description": "The profile in profiles/<name>.json to use."
    },
    "api_registry": {
      "type": "object",
      "properties": {
        "ollama": {
          "type": "object",
          "properties": {
            "endpoint": {
              "type": "string",
              "description": "Where Ollama listens. Defaults to http://localhost:11434."
            },
            "base_url": {
              "type": "string"
            },
            "num_ctx": {
              "type": "integer",
              "exclusiveMinimum": 0,
              "maximum": 9007199254740991,
              "description": "The context window Ollama allocates for every request."
            }
          },
          "additionalProperties": false
        },
        "openai": {
          "type": "object",
          "properties": {
            "api_key": {
              "type": "string",
              "description": "A key stored in the file. Prefer key_env_var, so the key stays out of files."
            },
            "key_env_var": {
              "type": "string",
              "description": "The environment variable holding the key."
            },
            "base_url": {
              "type": "string",
              "description": "The API base URL."
            }
          },
          "additionalProperties": false
        },
        "anthropic": {
          "type": "object",
          "properties": {
            "api_key": {
              "type": "string",
              "description": "A key stored in the file. Prefer key_env_var, so the key stays out of files."
            },
            "key_env_var": {
              "type": "string",
              "description": "The environment variable holding the key."
            },
            "base_url": {
              "type": "string",
              "description": "The API base URL."
            }
          },
          "additionalProperties": false
        },
        "openrouter": {
          "type": "object",
          "properties": {
            "api_key": {
              "type": "string",
              "description": "A key stored in the file. Prefer key_env_var, so the key stays out of files."
            },
            "key_env_var": {
              "type": "string",
              "description": "The environment variable holding the key."
            },
            "base_url": {
              "type": "string",
              "description": "The API base URL."
            },
            "referer": {
              "type": "string",
              "description": "Sent as HTTP-Referer, for OpenRouter attribution."
            },
            "title": {
              "type": "string",
              "description": "Sent as X-Title, for OpenRouter attribution."
            }
          },
          "additionalProperties": false
        },
        "endpoints": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1,
                "description": "The provider name models are addressed by, as in \"id:model\"."
              },
              "base_url": {
                "type": "string",
                "minLength": 1
              },
              "dialect": {
                "description": "The wire format. Defaults to openai.",
                "type": "string",
                "enum": [
                  "openai",
                  "anthropic"
                ]
              },
              "api_key": {
                "type": "string",
                "description": "A key stored in the file. Prefer key_env_var, so the key stays out of files."
              },
              "key_env_var": {
                "type": "string",
                "description": "The environment variable holding the key."
              },
              "headers": {
                "description": "Headers sent with every request.",
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {
                  "type": "string"
                }
              }
            },
            "required": [
              "id",
              "base_url"
            ],
            "additionalProperties": false
          },
          "description": "Other OpenAI or Anthropic compatible endpoints."
        }
      },
      "additionalProperties": false,
      "description": "Where each provider is and how to authenticate to it."
    },
    "models": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "object",
        "properties": {
          "context_window": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991,
            "description": "Tokens the model accepts in one request."
          },
          "max_output": {
            "type": "integer",
            "exclusiveMinimum": 0,
            "maximum": 9007199254740991,
            "description": "Most tokens the model writes in one reply."
          },
          "tools": {
            "type": "boolean"
          },
          "reasoning": {
            "type": "boolean"
          },
          "images": {
            "type": "boolean"
          },
          "thinking": {
            "type": "string",
            "enum": [
              "adaptive",
              "budget"
            ]
          },
          "always_thinks": {
            "type": "boolean"
          },
          "effort": {
            "type": "boolean"
          },
          "price": {
            "type": "object",
            "properties": {
              "input": {
                "type": "number",
                "minimum": 0
              },
              "output": {
                "type": "number",
                "minimum": 0
              },
              "cache_read": {
                "type": "number",
                "minimum": 0
              },
              "cache_write": {
                "type": "number",
                "minimum": 0
              }
            },
            "required": [
              "input",
              "output"
            ],
            "additionalProperties": false,
            "description": "US dollars per million tokens."
          }
        },
        "additionalProperties": false
      },
      "description": "Facts about models, keyed \"provider:model\", overriding what providers report."
    },
    "permissions": {
      "type": "object",
      "properties": {
        "allow": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Rules that allow a call without asking, such as \"run_command(npm test *)\". Lists from every layer apply."
        },
        "ask": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Rules that ask before a call, such as \"run_command(npm test *)\". Lists from every layer apply."
        },
        "deny": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Rules that refuse a call; a deny always wins, such as \"run_command(npm test *)\". Lists from every layer apply."
        },
        "mode": {
          "type": "string",
          "enum": [
            "plan",
            "default",
            "accept-edits",
            "auto",
            "bypass"
          ],
          "description": "The mode a session starts in."
        }
      },
      "additionalProperties": false
    },
    "sandbox": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "On by default wherever a sandbox works."
        },
        "network": {
          "type": "boolean",
          "description": "Let sandboxed commands reach the network. Off by default."
        },
        "writable": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Directories besides the project and the temporary directory that commands may write."
        },
        "hidden": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Paths hidden from commands in addition to the defaults."
        },
        "env": {
          "type": "string",
          "enum": [
            "scrubbed",
            "minimal"
          ],
          "description": "How much of the environment commands and servers inherit."
        },
        "env_passthrough": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Variables passed to every process by name."
        }
      },
      "additionalProperties": false
    },
    "agent_loop": {
      "type": "object",
      "properties": {
        "max_steps": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991,
          "description": "Model requests per user turn before the loop stops."
        },
        "max_tool_calls_per_turn": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Tool calls per turn; 0 means no cap beyond the step limit."
        },
        "tool_result_max_chars": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991,
          "description": "Characters of one tool result kept in context."
        },
        "command_timeout_ms": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991,
          "description": "Default timeout for a command."
        },
        "max_output_tokens": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991,
          "description": "Output tokens to ask for per reply, capped by the model's own limit."
        }
      },
      "additionalProperties": false
    },
    "lsp": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "The lsp tool and diagnostics after edits. On by default, for the servers that are installed."
        },
        "diagnostics_after_edit": {
          "type": "boolean",
          "description": "Tell the model about errors the language server finds in a file it just changed. On by default."
        },
        "servers": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {
            "type": "object",
            "properties": {
              "command": {
                "type": "string",
                "minLength": 1
              },
              "args": {
                "type": "array",
                "items": {
                  "type": "string"
                }
              },
              "extensions": {
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "description": "File extensions it serves, without the dot."
              },
              "enabled": {
                "type": "boolean"
              }
            },
            "required": [
              "command",
              "extensions"
            ],
            "additionalProperties": false
          },
          "description": "Language servers by name. A name used by a default (typescript, python, go, rust) replaces it."
        }
      },
      "additionalProperties": false
    },
    "tool_search": {
      "type": "object",
      "properties": {
        "threshold": {
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991,
          "description": "Offer MCP tools through search_tools once the servers bring more than this many. 0 always sends them all. Defaults to 40."
        }
      },
      "additionalProperties": false
    },
    "context": {
      "type": "object",
      "properties": {
        "auto_compact": {
          "type": "boolean",
          "description": "Summarize older turns when a request nears the model's window. On by default."
        }
      },
      "additionalProperties": false
    },
    "categories": {
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "model": {
              "type": "string",
              "minLength": 1
            },
            "reasoning": {
              "type": "string",
              "enum": [
                "off",
                "on",
                "auto"
              ]
            }
          },
          "required": [
            "model"
          ],
          "additionalProperties": false
        }
      },
      "description": "Model chains delegation routes each category of task to."
    },
    "delegation": {
      "type": "object",
      "properties": {
        "max_depth": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "max_concurrent": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "max_turns_per_child": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        }
      },
      "additionalProperties": false
    },
    "trust": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "model": {
          "type": "string"
        },
        "threshold": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "dedupe": {
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "description": "The classifier that screens tool output before the model reads it."
    },
    "telemetry": {
      "type": "boolean",
      "description": "The legacy interface's telemetry switch. Traces are sent only when otel.enabled is true."
    },
    "otel": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean",
          "description": "Send traces to an OpenTelemetry collector. Off by default: nothing leaves the machine unless this is true."
        },
        "endpoint": {
          "type": "string",
          "description": "The OTLP/HTTP traces URL. Defaults to OTEL_EXPORTER_OTLP_TRACES_ENDPOINT, OTEL_EXPORTER_OTLP_ENDPOINT, then http://localhost:4318/v1/traces."
        },
        "headers": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {
            "type": "string"
          },
          "description": "Headers sent with every export, such as a collector token."
        },
        "include_content": {
          "type": "boolean",
          "description": "Put prompts, outputs, tool arguments, and tool results on spans. Off by default."
        }
      },
      "additionalProperties": false
    },
    "hooks": {
      "type": "object",
      "properties": {
        "session_start": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        },
        "user_prompt_submit": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        },
        "pre_tool": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        },
        "post_tool": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        },
        "stop": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        },
        "pre_compact": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        },
        "notification": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        },
        "session_end": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "matcher": {
                "description": "For pre_tool and post_tool: a rule such as run_command(git push*) or edit. Every call when absent.",
                "type": "string"
              },
              "command": {
                "type": "string",
                "minLength": 1,
                "description": "Run with /bin/sh (cmd.exe on Windows), reading the event as JSON on standard input."
              },
              "timeout_ms": {
                "description": "Stop it and report a failure after this long. Defaults to 30000.",
                "type": "integer",
                "exclusiveMinimum": 0,
                "maximum": 9007199254740991
              },
              "enabled": {
                "description": "false keeps it here without running it.",
                "type": "boolean"
              }
            },
            "required": [
              "command"
            ],
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false,
      "description": "Commands run at lifecycle events. Exit 0 continues, exit 2 blocks with standard error as the reason. A project's hooks run only once trusted."
    },
    "git": {
      "type": "object",
      "properties": {
        "attribution": {
          "type": "string",
          "description": "A trailer added to every commit JamCLI makes, such as \"Co-authored-by: ...\". Off by default: commits carry only your own identity."
        },
        "allow_commit_in_bypass": {
          "type": "boolean",
          "description": "Let bypass mode commit without asking. Off by default: a commit is always asked for, whatever the mode."
        }
      },
      "additionalProperties": false,
      "description": "How JamCLI commits."
    },
    "ui": {
      "type": "object",
      "properties": {
        "theme": {
          "type": "string",
          "enum": [
            "dark",
            "light",
            "high-contrast",
            "monochrome"
          ],
          "description": "The interface's colors. NO_COLOR forces monochrome. Defaults to dark."
        },
        "screen_reader": {
          "type": "boolean",
          "description": "Draw the interface as plain labeled lines, with no boxes, marks, or animation. --screen-reader turns it on for one run."
        },
        "reduced_motion": {
          "type": "boolean",
          "description": "Stop the spinner and shimmer. On by itself in screen reader mode."
        },
        "status_text_style": {
          "type": "string",
          "pattern": "^(subtle|rainbow|minimal|aurora|mono|custom:[\\w.-]+)$",
          "description": "How the working indicator's words shimmer: subtle, rainbow, minimal, aurora, mono, or custom:<name>. Defaults to subtle."
        },
        "status_spinner_style": {
          "type": "string",
          "pattern": "^(classic|orbit|pulse|big_classic|big_orbit|big_pulse|custom:[\\w.-]+)$",
          "description": "The working indicator's spinner: classic, orbit, pulse, big_classic, big_orbit, big_pulse, or custom:<name>. Defaults to classic."
        },
        "custom_status_styles": {
          "type": "object",
          "propertyNames": {
            "type": "string"
          },
          "additionalProperties": {
            "type": "object",
            "properties": {
              "path": {
                "type": "string",
                "description": "A JSON file with label, shimmerColors, shimmer, spinnerFrames, spinnerColors, and spinnerIntervalMs."
              },
              "label": {
                "type": "string"
              }
            },
            "required": [
              "path"
            ],
            "additionalProperties": false
          },
          "description": "Styles of your own, by name, each read from a JSON file. Name one as custom:<name>."
        }
      },
      "additionalProperties": false,
      "description": "How the interface looks and moves."
    },
    "context_management": {
      "type": "object",
      "properties": {
        "enabled": {
          "type": "boolean"
        },
        "max_tokens": {
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "compression_threshold": {
          "type": "number",
          "minimum": 0,
          "maximum": 1
        },
        "strategy": {
          "type": "string",
          "enum": [
            "summarize",
            "truncate"
          ]
        }
      },
      "additionalProperties": false,
      "description": "Context settings for the legacy interface. Sessions use context instead."
    },
    "general": {
      "type": "object",
      "properties": {
        "show_tool_calling_models_only": {
          "type": "boolean"
        }
      },
      "additionalProperties": false
    },
    "available_models": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "provider": {
            "type": "string",
            "enum": [
              "ollama",
              "openai",
              "anthropic",
              "openrouter"
            ]
          },
          "name": {
            "type": "string"
          },
          "description": {
            "type": "string"
          },
          "supports_tool_calling": {
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "provider",
          "name"
        ],
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false,
  "title": "JamCLI configuration"
}
